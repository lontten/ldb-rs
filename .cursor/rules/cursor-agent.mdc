---
description: Cursor Agent 与 Plan 模式协作约定
alwaysApply: true
---

# Cursor Agent 协作

## Plan 模式（默认）

用户选用 Plan 模式时，流程为：**先列计划 → 用户审阅 → 手动点 Build 执行**。

- **只维护计划**：更新 `.cursor/plans/*.plan.md`，写清改哪些文件、不改什么、验证步骤、todos
- **不执行实现**：不改代码、不跑会改变仓库状态的命令，除非用户已点 Build 或明确退出 Plan 模式并要求执行
- **不提示切换模式**：用户描述实现内容或说「更新代码」，在 Plan 模式下视为 **plan iteration**，更新计划即可
- **迭代即改计划**：补充需求、纠正方案 → 反映到计划（必要时 AskQuestion）

### Plan 模式硬约束

- **禁止**调用 `SwitchMode` 或任何形式请求用户「切换到 Agent」；执行权仅由用户点 **Build** 获得
- **仅可编辑**：`.cursor/plans/*.plan.md`；只读查看其他文件
- **禁止编辑**：源码、`.mdc` 规则、CI、除计划外的任何会改仓库的文件（Plan 模式下 Cursor 产品层亦会拦截非 markdown）
- 用户要求改规则 / 代码 / 提交：视为 **plan iteration**，把具体改动写入当前计划（含文件路径与条文），**一句**提醒「请点 Build 执行」；不尝试写入、不因失败而请求切模式
- 工具调用被系统拦截时：**停止**，把未完成项记入计划 todos；**不**重试 SwitchMode、不解释产品机制长篇大论

### 何时才算执行

用户 **明确** 要求执行且 **已点 Build / 处于 Agent 模式** 时，才改代码并验证。

## Agent 模式（Build 之后）

- 按 todos 顺序执行，标记 in_progress → completed
- 不扩大范围；遵守计划中的「不改动的部分」
- Rust 改动按 `rust-verify`：`cargo fmt`、`cargo test`

## 计划文档

- 步骤 actionable，引用具体路径；已有 todos 不重复创建
- 用户说 do NOT edit the plan file 时，不修改该 plan 源文件

## 与本仓库其他规则

- 实现细节见 `general-common.mdc`、`general-rust.mdc`、`project-conventions.mdc`
- Git commit / push 入口与流程见 `git-workflow.mdc`（仅 Diff 面板快捷按钮，聊天禁用）
- 计划中的约定（如 `field_name`、变体级 `///`）落地时须与上述规则一致

## 项目暴露面与协作内容分界

### 仅允许写在 `.cursor/` 内

- Agent / Plan 协作说明、交接稿、路线图、Plan 产物
- 对 `.cursor` 路径的引用与协作工具用语（`rules/`、`skills/` 随仓库提交）

### 项目暴露面（会进 Git 或对外可见）

下列范围**不得**出现 `Cursor`、`Agent`、`AI`、`HANDOFF`、IDE Plan 模式等协作用语，亦不得以说明协作配置为目的引用 `.cursor/`、`.cursorignore`：

- `README.md`、`doc/**`、源码注释、`Cargo.toml` description
- **Git commit message**、PR 标题与正文、CHANGELOG
- `.gitignore`、`.cursorignore` 中的**注释**（须中性表述）

### Commit message 示例

| 避免 | 推荐 |
|------|------|
| `补充 Agent 交接文档` | `docs: 补充 README 与 doc 占位` |

### 其它

- 临时计划只维护在 `.cursor/plans/`（已 gitignore），不得写入 `doc/`
- **`.cursor/` 的 `rules/`、`skills/` 随仓库提交**；协作说明集中在本目录
- 违反分界时：改写到 `.cursor/` 或删除对外文案；**不要**在 commit message 中解释协作工具或本目录
