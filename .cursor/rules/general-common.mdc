---
description: 跨语言通用命名与术语约定
alwaysApply: true
---

# 通用约定

## 规则文件组织

- `general-common.mdc`：与语言无关的通用原则
- `general-{lang}.mdc`：仅该语言特有约定（如 `_list` / `List`）
- `project-conventions.mdc`：当前项目自定义，复制到其他项目后改内容
- `cursor-agent.mdc`：Cursor Plan / Agent 模式协作（先计划、Build 后执行）
- `git-workflow.mdc`：Git commit / push 仅 UI 快捷按钮触发

新增或调整规则时，按上述分层放入对应文件。

## 集合命名

- 集合/列表字段用显式后缀，不用英文复数（`s` / 不规则变形）
- 后缀形式随语言命名风格适配，见各 `general-{lang}.mdc`

## 术语

- 不使用 `schema` 表示字段/结构定义（跨系统歧义大）

## 命名

- 模块名/包名与字段名不重复，避免同名不同义
- **HTTP / 接口入参 DTO**（反序列化用、未落内部模型前）：类型名用 **`{领域}{实体}Input`** 或 **`{实体}Input`**，与内部层结构一眼区分
- **避免**：`Api*` 前缀（易当成客户端或 endpoint）、`Json` / `Dto` 等序列化格式后缀
- **内部层**（如 Segment `Field`）：无 `Input` 后缀；层级由模块名表达

## 注释

- 结构体/枚举：类型上方 1～2 行，说明职责与使用场景
- **标识符保持英文**（代码与 API 契约）；**注释用中文**，降低阅读门槛
- 避免：零注释的复杂结构体；复述代码的废话注释；过多注释分散注意力

### 分层原则

| 类型 | 是否注释 | 语言与内容 |
|------|----------|------------|
| 错误枚举 `*Error` | **每个变体必须** | 中文：何时触发、对应哪条校验 |
| 带字段的变体 | **变体级 `///` 为主** | 在变体注释中说明各字段含义；自明或与变体说明重复时不必逐字段 `///` |
| 对外 API 入参结构体 | 类型 + 每个 pub 字段 | 中文说明 JSON 语义 |
| 自明局部变量、`derive` | 不注释 | — |

普通结构体字段：仅对非显然字段注释（设计意图、应用场景、与其他字段关系）；自明字段（如内部临时变量）不注释。
